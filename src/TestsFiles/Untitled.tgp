-- myLexCode: es un codigo de prueba en lenguaje TagaPlate

Proc @Principal(
	MoveRight;
	MoveLeft;
	Hammer(S);
	New @nuevo, (Bool, True);
	New @justin, (Num, 1800);



	Case @justin 
		When 1 Then 
			(Stop;)
		When 18 Then 
			(MoveLeft;);

);