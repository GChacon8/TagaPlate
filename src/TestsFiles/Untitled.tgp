-- myLexCode: es un codigo de prueba en lenguaje TagaPlate

Proc @Principal(
	MoveRight;
	MoveLeft;
	Hammer(S);
	New @nuevo, (Bool, True);
	New @justin, (Num, 1800);

	PrintValues(88,"HOLA",@nuevo);
	Case When (IsTrue(@nuevo)) Then
		(MoveRight;)
	Else
		(MoveLeft;);
	
);